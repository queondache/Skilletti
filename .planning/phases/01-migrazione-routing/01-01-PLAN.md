---
phase: 01-migrazione-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/steps.ts
  - components/StepNav.tsx
  - components/SiteHeader.tsx
  - components/SiteFooter.tsx
  - app/layout.tsx
  - app/page.tsx
  - app/step-1-capisci/page.tsx
  - app/step-2-installa/page.tsx
  - app/step-3-prime-skill/page.tsx
  - app/step-4-esplora/page.tsx
  - app/step-5-costruisci/page.tsx
autonomous: true
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-04, STRUCT-05, STRUCT-06]
must_haves:
  truths:
    - "Il sito espone 6 route static-export: / + /step-1-capisci + /step-2-installa + /step-3-prime-skill + /step-4-esplora + /step-5-costruisci."
    - "I contenuti v1 sono MIGRATI (non riscritti): pedagogia.mdx→step-1, ComeIniziare→step-2, essenziali→step-3, Catalog/Summary/ContextFilter→step-4, workflow.mdx+TemplateSection→step-5."
    - "Header persistente = solo wordmark 'skilletti.' → home (NavSticky a 6 ancore ritirato). Footer globale = bio placeholder + LinkedIn + 'chatta con Claude' → claude.ai, no link repo."
    - "Ogni step route ha StepNav in fondo: ‹ Indietro / Avanti › + 'Step X di 5 — Nome'. No breadcrumb, no pallini. Step 5 → closing card."
    - "Ogni route ha metadata (title/description/canonical/OG). tsc + build + validate:data verdi."
  artifacts:
    - path: "lib/steps.ts"
      provides: "Config dei 5 step (slug, n, label, href) — single source per StepNav e landing map"
      exports: ["STEPS", "type Step"]
    - path: "components/StepNav.tsx"
      provides: "Server component: prev/next + 'Step X di 5 — Nome' da STEPS"
      exports: ["StepNav"]
    - path: "components/SiteHeader.tsx"
      provides: "Header minimale wordmark → home"
      exports: ["SiteHeader"]
    - path: "components/SiteFooter.tsx"
      provides: "Footer globale: bio placeholder + LinkedIn + chatta con Claude"
      exports: ["SiteFooter"]
    - path: "app/page.tsx"
      provides: "Landing: hero placeholder 50-80 parole + CTA Inizia + mappa 5 step"
    - path: "app/step-4-esplora/page.tsx"
      provides: "Catalogo + ContextFilter + Summary migrati"
  key_links:
    - from: "components/StepNav.tsx"
      to: "lib/steps.ts STEPS"
      via: "import"
      pattern: "import.*STEPS"
    - from: "app/layout.tsx"
      to: "components/SiteHeader.tsx + components/SiteFooter.tsx"
      via: "render chrome globale"
      pattern: "SiteHeader|SiteFooter"
---

<objective>
Migrare Skilletti da single-page (app/page.tsx monolitico) a 6 route App Router (static export),
spostando i contenuti v1 nelle route corrette SENZA riscriverli. Costruire la chrome "wizard
minimale": header solo-wordmark, footer globale, step-navigator prev/next. Stile estetico = quello
v1 esistente (il reskin alla direzione "Terminale editoriale" è Fase C — NON qui).

Output: 5 route step + landing nuova + StepNav + SiteHeader + SiteFooter + lib/steps.ts; NavSticky
ritirato dal layout. Niente modifiche a skills.json/schema. Nessuna nuova dipendenza npm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/round6-blueprints.md  (Fase B = contratto)
@.claude/round6-mockups/DIRECTION-LOCKED.md  (token, ma applicazione visiva = Fase C)

<interfaces>
Da app/page.tsx (monolite attuale — fonte della migrazione):
- legge a build time: content/workflow.mdx, content/pedagogia.mdx (con replace h1/##→#),
  content/templates/claude-base.md, content/templates/spec-base.md
- `skills = skillsData as Skill[]` (da @/data/skills.json, tipo @/types/skill)
- `partiDaQui` = skills.filter(importanza==='essenziale').slice(0,5)
- componenti usati: Hero, Summary(skills), SkillCard(skill, variant), Catalog(skills),
  ComeIniziare, TemplateSection(claudeMd, specMd), Article (da @/lib/markdown)
- pattern sezione: wrapper con classi pl-[var(--gutter-indent)] pr-[...] py-12 sm:py-20 border-t border-rule,
  kicker uppercase + h2 [clamp...] + lead italic

Da app/layout.tsx: monta NavSticky + Reveal nel body; metadata centralizzata (SITE_URL, TITLE, DESCRIPTION).
Da next.config.ts: output:'export', trailingSlash:true, basePath condizionale (Vercel='' ⇒ root).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/steps.ts + StepNav</name>
  <files>lib/steps.ts, components/StepNav.tsx</files>
  <read_first>
    - components/Wordmark.tsx (per coerenza wordmark)
    - app/page.tsx (pattern classi sezione)
  </read_first>
  <action>
    Crea lib/steps.ts: `export type Step = { n: number; slug: string; label: string; href: string }`
    + `export const STEPS: Step[]` coi 5 step (1 Capisci /step-1-capisci, 2 Installa /step-2-installa,
    3 Prime skill /step-3-prime-skill, 4 Esplora /step-4-esplora, 5 Costruisci /step-5-costruisci).
    Crea components/StepNav.tsx (server component) che riceve `current: number` e rende: indicatore
    "Step {n} di 5 — {label}", link ‹ Indietro (step n-1, o Home se n=1) e Avanti › (step n+1).
    Per n=5 l'Avanti diventa una closing card/CTA ("Hai finito il percorso — torna all'inizio" +
    link "chatta con Claude" → https://claude.ai). No breadcrumb, no pallini. Stile sobrio coerente v1.
  </action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>STEPS esportato; StepNav rende prev/next + indicatore; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: SiteHeader + SiteFooter + layout chrome</name>
  <files>components/SiteHeader.tsx, components/SiteFooter.tsx, app/layout.tsx</files>
  <read_first>
    - components/Wordmark.tsx
    - components/NavSticky.tsx (cosa ritiriamo)
    - app/layout.tsx
  </read_first>
  <action>
    SiteHeader.tsx: header minimale persistente con solo il wordmark "skilletti." linkato a "/".
    Niente menu, niente ancore. Sticky leggero opzionale.
    SiteFooter.tsx: footer globale con bio Andrea PLACEHOLDER breve (1-2 righe, segnaposto, Andrea
    riscrive in Round 7), link "LinkedIn" (href placeholder #), e "Se ti serve aiuto, chatta con
    Claude" → https://claude.ai. NESSUN link al repo.
    layout.tsx: sostituisci `<NavSticky />` con `<SiteHeader />`; mantieni `<Reveal />`; rendi
    `<SiteFooter />` dopo `{children}`. Lascia il file NavSticky.tsx sul disco ma non importarlo.
  </action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Header wordmark + footer globale montati nel layout; NavSticky non più importato; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 3: Landing / (nuova)</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (monolite — da svuotare)
    - components/Hero.tsx (cosa ritiriamo dall'hero v1)
    - lib/steps.ts
  </read_first>
  <action>
    Riscrivi app/page.tsx come LANDING: hero con wordmark, titolo placeholder, paragrafo placeholder
    ~50-80 parole IT plausibile su cos'è Claude Code (Andrea riscrive in Round 7), CTA primaria
    "Inizia" → /step-1-capisci, link secondario "Cos'è una skill?" → /step-1-capisci.
    Sotto, una **mappa dei 5 step** = lista/overview cliccabile da STEPS (ogni voce: indice + label +
    una riga placeholder). Cliccabile (deep-link). Rimuovi tutte le vecchie sezioni del monolite
    (Summary, parti-da-qui, come-iniziare, catalogo, workflow, template, didattica, footer interno):
    quei contenuti vivono ora nelle route step. Stile: riusa le classi/utility v1 esistenti (il reskin
    è Fase C). Aggiungi `export const metadata` per la landing (title/description).
  </action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>app/page.tsx = landing hero placeholder + CTA + mappa 5 step; vecchie sezioni rimosse; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 4: Route step-1-capisci + step-2-installa</name>
  <files>app/step-1-capisci/page.tsx, app/step-2-installa/page.tsx</files>
  <read_first>
    - components/ComeIniziare.tsx
    - lib/markdown (Article)
  </read_first>
  <action>
    step-1-capisci/page.tsx: server component che legge content/pedagogia.mdx (stesso replace
    h1/##→# del monolite) e lo rende via Article, dentro il pattern sezione v1 (kicker "capisci",
    h2 "Un piccolo vocabolario"). In fondo `<StepNav current={1} />`. `export const metadata`
    (title "Capisci", description, canonical '/step-1-capisci', openGraph).
    step-2-installa/page.tsx: rende `<ComeIniziare />` nel pattern sezione (kicker "installa",
    h2 "Come iniziare"). `<StepNav current={2} />`. metadata analoga.
  </action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>step-1 e step-2 rendono i contenuti migrati + StepNav + metadata; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 5: Route step-3-prime-skill + step-4-esplora</name>
  <files>app/step-3-prime-skill/page.tsx, app/step-4-esplora/page.tsx</files>
  <read_first>
    - components/SkillCard.tsx (props, variant)
    - components/Catalog.tsx, components/Summary.tsx, components/ContextFilter.tsx
    - types/skill, data/skills.json (shape)
  </read_first>
  <action>
    step-3-prime-skill/page.tsx: carica skills da @/data/skills.json, filtra essenziali (slice 5),
    rende la griglia di SkillCard (variant="doorway") nel pattern sezione (kicker "prime skill",
    h2 "Le essenziali" + lead). `<StepNav current={3} />`. metadata.
    step-4-esplora/page.tsx: carica skills, rende `<Catalog skills={skills} />` (che già include i
    suoi controlli) + `<Summary skills={skills} />` come oggi. NB: la SOSTITUZIONE del Summary con la
    mappa parole è Fase D — qui si MIGRA com'è. `<StepNav current={4} />`. metadata.
  </action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>step-3 (essenziali) e step-4 (catalogo+summary+filtro) migrati + StepNav + metadata; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 6: Route step-5-costruisci</name>
  <files>app/step-5-costruisci/page.tsx</files>
  <read_first>
    - components/TemplateSection.tsx
    - lib/markdown (Article)
  </read_first>
  <action>
    step-5-costruisci/page.tsx: legge content/workflow.mdx → Article (kicker "metodo", h2 "Come uso
    Claude.ai e Claude Code insieme") e legge content/templates/claude-base.md + spec-base.md →
    `<TemplateSection claudeMd specMd />` (kicker "template", h2 "Due file per partire"). In fondo
    `<StepNav current={5} />` (che rende la closing card per lo step finale). metadata.
  </action>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>step-5 rende Metodo + Template + closing card; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 7: Verifica build + export 6 route</name>
  <files>(verifica)</files>
  <action>
    Esegui `npm run validate:data`, `npx tsc --noEmit`, e `npm run build` (static export).
    Verifica che out/ contenga index.html + step-1-capisci/ … step-5-costruisci/ (6 route).
    Correggi eventuali errori di tipo/import emersi dalla migrazione (es. import non più usati,
    Article path, metadata types). NON cambiare skills.json/schema. NON aggiungere dipendenze.
  </action>
  <verify><automated>npm run validate:data && npx tsc --noEmit && npm run build</automated></verify>
  <done>validate:data + tsc + build verdi; out/ ha le 6 route esportate.</done>
</task>

</tasks>

<verification>
- `npm run validate:data` — verde (skills.json invariato).
- `npx tsc --noEmit` — verde.
- `npm run build` — export OK; out/ contiene le 6 route (index + 5 step).
- Header = solo wordmark; footer globale presente; StepNav prev/next su ogni step; landing con CTA + mappa 5 step.
</verification>

<success_criteria>
- 6 route App Router static-export funzionanti, contenuti v1 migrati senza riscrittura.
- Chrome wizard minimale (header wordmark, footer globale, StepNav) in piedi.
- skills.json/schema intoccati; nessuna nuova dipendenza; build statica verde.
</success_criteria>

<output>
Crea `.planning/phases/01-migrazione-routing/01-01-SUMMARY.md` a fine fase con: route create, file
ritirati (NavSticky import), eventuali deviazioni minori, stato build/tsc/validate.
</output>
