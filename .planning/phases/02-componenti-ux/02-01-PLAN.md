---
phase: 02-componenti-ux
plan: 01
type: execute
wave: 1
depends_on: ["01-migrazione-routing"]
files_modified:
  - app/fonts.ts
  - app/globals.css
  - app/page.tsx
  - app/step-2-installa/page.tsx
  - components/StepNav.tsx
  - components/SiteFooter.tsx
  - components/HeroMap.tsx
  - components/TermBlock.tsx
autonomous: true
requirements: [DESIGN-03, DESIGN-04]
must_haves:
  truths:
    - "Direzione 'Terminale editoriale' raffinata applicata: token palette aggiornati (accento #C2563A, hairline #EAE4D8, radius 10), Inter Tight per CTA/micro-label, JetBrains Mono per comandi."
    - "Hero landing = mappa-testo CSS: 6 temi-parola con linee hairline (anteprima di /step-4-esplora), CSS/SVG only, niente canvas/JS pesante."
    - "Blocco terminale (motivo command-line) presente in /step-2-installa."
    - "tsc + build + validate:data verdi; reduced-motion rispettato."
  artifacts:
    - path: "components/HeroMap.tsx"
      provides: "Mappa-testo hero: 6 nodi-tema + connettori hairline (SVG/CSS)"
      exports: ["HeroMap"]
    - path: "components/TermBlock.tsx"
      provides: "Blocco terminale stile editoriale (barra + ❯ comando)"
      exports: ["TermBlock"]
---

<objective>
Applicare la direzione estetica BLOCCATA (Fase A — "Terminale editoriale" raffinata Apple-clean) ai
componenti chiave. Token in .claude/round6-mockups/DIRECTION-LOCKED.md. NON cambiare la struttura
(Fase B) né introdurre la mappa-parole interattiva (Fase D) né animazioni nuove (Fase E).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.claude/round6-mockups/DIRECTION-LOCKED.md
</execution_context>

<tasks>
<task type="auto"><name>T1: fonts.ts — Inter Tight + JetBrains Mono</name>
<action>Aggiungi Inter_Tight (var --font-inter-tight, wght 500/600/700) e JetBrains_Mono (var --font-jetbrains). Mantieni Fraunces. Esponi le variabili nel className di html (layout.tsx già fa ${fraunces.variable} ecc. → aggiungere le nuove). Geist può restare come fallback ma non è più primario.</action>
<verify><automated>npx tsc --noEmit</automated></verify></task>

<task type="auto"><name>T2: globals.css — token direzione</name>
<action>Aggiorna @theme: --color-paper #F8F5EF, --color-ink #1A1714, --color-terracotta #C2563A, --color-terracotta-deep #A8472C, --color-rule #EAE4D8, --color-muted #6E685D. --font-body → Fraunces (body voce). Aggiungi --font-sans = Inter Tight (CTA/label) e --font-mono = JetBrains Mono. Aggiungi --radius: 10px. Mantieni reveal/reduced-motion. Ombre soft (niente stamp).</action>
<verify><automated>npx tsc --noEmit</automated></verify></task>

<task type="auto"><name>T3: Inter Tight su kicker/CTA + radius</name>
<action>Applica font-sans (Inter Tight) ai kicker uppercase + CTA + StepNav label + footer label. CTA "Inizia" radius var(--radius). Coerenza coi token.</action>
<verify><automated>npx tsc --noEmit</automated></verify></task>

<task type="auto"><name>T4: HeroMap (mappa-testo) in landing</name>
<action>components/HeroMap.tsx: 6 temi-parola (Metodo, Frontend, Sicurezza, Dati, Scrittura, Ricerca) disposti con connettori hairline sottili (SVG o CSS), wordmark/centro implicito, link a /step-4-esplora. Inseriscilo nell'hero di app/page.tsx (accanto/sotto il copy). CSS-only, niente animazione (Fase E).</action>
<verify><automated>npx tsc --noEmit</automated></verify></task>

<task type="auto"><name>T5: TermBlock in /step-2-installa</name>
<action>components/TermBlock.tsx: blocco terminale (barra finestra + prompt ❯ comando mono). Mostralo in cima a step-2-installa come motivo-firma (es. `❯ npm i -g @anthropic-ai/claude-code`). Non duplicare i contenuti di ComeIniziare; è un elemento decorativo-introduttivo.</action>
<verify><automated>npx tsc --noEmit</automated></verify></task>

<task type="auto"><name>T6: build + verifica</name>
<action>npm run build (export) + verifica visiva landing (hero mappa-testo) e installa (terminale). Correggi errori.</action>
<verify><automated>npm run validate:data && npx tsc --noEmit && npm run build</automated></verify></task>
</tasks>

<verification>
- tsc + build + validate verdi; 6 route esportate.
- Landing: hero con mappa-testo connessa; CTA Inter Tight + radius.
- /step-2-installa: blocco terminale presente.
- Token direzione applicati (accento #C2563A, hairline più chiara, radius).
</verification>

<success_criteria>
- Direzione "Terminale editoriale" raffinata applicata a hero, card, nav, footer.
- Hero mappa-testo = anteprima di step-4; terminale su installa; CSS/SVG only.
</success_criteria>

<output>Crea 02-01-SUMMARY.md a fine fase.</output>
